table:
  name: services
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - id
        - salon_id
        - name
        - description
        - duration_minutes
        - price_cents
      filter:
        _and:
          - active:
              _eq: true
          - published:
              _eq: true
